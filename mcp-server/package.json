{
  "name": "json-scenes-for-unity-mcp",
  "version": "1.0.0",
  "description": "MCP server companion for json-scenes-for-unity. Handles entity creation (UUID generation), force_reload, and validate_scene commands.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
