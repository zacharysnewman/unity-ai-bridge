{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(jq -r '.tool_input.file_path // empty' 2>/dev/null); echo \"$FILE\" | grep -qE '(\\.claude/commands/.*\\.md|CLAUDE\\.md)$' && echo '{\"systemMessage\":\"Claude integration file modified — re-run /install-json-scenes to propagate changes to any Unity project using this package.\"}' || true",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
